import { Description, Title } from '~/components'

<Title>sign out</Title>
<Description>
  logout is a client-side function that makes a request to the server in order
  to clear the session cookie.
</Description>

# logout

logout is a client-side function that makes a request to the server in order
to clear the session cookie.

```ts
authClient.logout({ redirectTo: '/account' })
```
